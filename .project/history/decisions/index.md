# Decisions

One file per durable decision: `NNNN-<slug>.md`, `type: decision`. A decision is written only when there was a REAL alternative — it records the discarded paths, not just the chosen one.

*Empty — nothing has been written under this layer yet.*
